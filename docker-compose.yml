services:
  videoenabler:
    build: .
    image: videoenabler:latest
    container_name: videoenabler
    ports:
      - "8080:80"        # Change 8080 to any free port on your NAS
    restart: unless-stopped
    # History is stored in browser localStorage — no server-side files needed
